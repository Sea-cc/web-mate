import { createStore } from 'vuex'

export default createStore({
    state: () => {
        return {
            userName: 'Emojis'
        }
    },
    getters: {},
    mutations: {},
    actions: {},
    modules: {}
})
