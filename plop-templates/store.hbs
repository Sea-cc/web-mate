import { defineStore } from 'pinia'
import type {  } from './types'
import LocalCache from '@/utils/cache'

export const {{name}}Store = defineStore('{{name}}', {
    state: () /* Type */ => {
        return {}
    },
    getters: {},
    actions: {}
})
